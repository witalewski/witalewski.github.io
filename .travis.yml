language: node_js
node_js:
  - 'node'

script: yarn build

deploy:
  provider: pages
  target_branch: master
  local_dir: public
  skip_cleanup: true
  github_token: $DEPLOY_TOKEN
  keep_history: true
  verbose: true
  on:
    branch: dev
